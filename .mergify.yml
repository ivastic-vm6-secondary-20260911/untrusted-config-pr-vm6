pull_request_rules:
  - name: vm6 simple-template single-pass control
    conditions:
      - author = ivastic-vm6-secondary-20260911
    actions:
      comment:
        message: "VM6_RENDER={{ title }}"
