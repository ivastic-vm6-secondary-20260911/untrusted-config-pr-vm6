pull_request_rules:
  - name: VM6 qualified GitHub Actions identity discriminator
    conditions:
      - check-success = @github-actions/vm6-qualified-check
    actions:
      comment:
        message: VM6_QUALIFIED_CHECK_IDENTITY_TRIGGERED
